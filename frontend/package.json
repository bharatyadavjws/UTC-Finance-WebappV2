{
  "name": "utc-frontend",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:agent": "npm run dev -w agent-app",
    "dev:crm": "npm run dev -w crm-app",
    "build:agent": "npm run build -w agent-app",
    "build:crm": "npm run build -w crm-app"
  },
  "dependencies": {
    "react-to-pdf": "^3.2.2"
  }
}
